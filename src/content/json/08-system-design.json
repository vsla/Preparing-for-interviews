{
  "slug": "08-system-design",
  "headerHtml": "<h2>8. System Design (Deep) <span class=\"topic-count\">7 topics</span></h2>",
  "introHtml": "<p class=\"section-intro\">System design interviews are open-ended and assess architecture thinking. You need to reason about scale, trade off consistency vs availability, choose storage engines, design for failure, and explain your decisions clearly. Practice the common archetypes: URL shortener, news feed, chat application, rate limiter.</p>",
  "topics": [
    {
      "summary": "8.1 Fundamentals",
      "bodyHtml": "<h4>Latency Numbers Every Engineer Should Know</h4>\n<table>\n  <tbody><tr><th>Operation</th><th>Latency</th></tr>\n  <tr><td>L1 cache reference</td><td>~0.5 ns</td></tr>\n  <tr><td>L2 cache reference</td><td>~7 ns</td></tr>\n  <tr><td>Main memory (RAM) access</td><td>~100 ns</td></tr>\n  <tr><td>SSD random read</td><td>~150 µs</td></tr>\n  <tr><td>HDD seek</td><td>~10 ms</td></tr>\n  <tr><td>Network same datacenter</td><td>~500 µs</td></tr>\n  <tr><td>Network cross-region</td><td>~30-100 ms</td></tr>\n  <tr><td>Network cross-continent</td><td>~150 ms</td></tr>\n</tbody></table>\n\n<h4>Horizontal vs Vertical Scaling</h4>\n<table>\n  <tbody><tr><th>Aspect</th><th>Vertical (Scale Up)</th><th>Horizontal (Scale Out)</th></tr>\n  <tr><td>Method</td><td>More RAM/CPU on same machine</td><td>More machines</td></tr>\n  <tr><td>Limit</td><td>Hardware ceiling</td><td>Near-limitless</td></tr>\n  <tr><td>Cost</td><td>Expensive at high end</td><td>Commodity hardware</td></tr>\n  <tr><td>Complexity</td><td>Simple</td><td>Distributed systems complexity</td></tr>\n  <tr><td>Downtime</td><td>Often required</td><td>Rolling deploys possible</td></tr>\n</tbody></table>\n\n<p><strong>Idempotency:</strong> same request produces same result regardless of how many times sent. Use idempotency keys on payment/mutation APIs: client sends unique key, server deduplicates.</p>"
    },
    {
      "summary": "8.2 Components to Know",
      "bodyHtml": "<h4>Load Balancers</h4>\n<pre><code>// L4 (Transport): routes by IP + TCP port — fast, no content awareness\n// L7 (Application): routes by HTTP headers, URL, cookies — smarter, SSL termination\n\n// Algorithms:\n// Round-robin: distribute evenly (default)\n// Weighted: more traffic to powerful servers\n// Least connections: route to server with fewest active connections\n// IP hash: same client always hits same server (session affinity)\n// Consistent hashing: minimal reshuffling when nodes added/removed</code></pre>\n\n<h4>Circuit Breaker Pattern</h4>\n<pre><code>// CLOSED: requests pass through, monitoring error rate\n// OPEN: all requests fail fast after threshold exceeded\n// HALF-OPEN: probe request; success closes, failure reopens\n\nclass CircuitBreaker {\n  constructor(fn, { threshold = 5, timeout = 60000 } = {}) {\n    this.fn = fn; this.state = \"CLOSED\"; this.failures = 0;\n    this.threshold = threshold; this.timeout = timeout;\n  }\n  async call(...args) {\n    if (this.state === \"OPEN\") throw new Error(\"Circuit OPEN\");\n    try {\n      const result = await this.fn(...args);\n      this.failures = 0; this.state = \"CLOSED\";\n      return result;\n    } catch (err) {\n      this.failures++;\n      if (this.failures &gt;= this.threshold) {\n        this.state = \"OPEN\";\n        setTimeout(() =&gt; { this.state = \"HALF-OPEN\"; }, this.timeout);\n      }\n      throw err;\n    }\n  }\n}</code></pre>"
    },
    {
      "summary": "8.3 Database Design at Scale",
      "bodyHtml": "<pre><code>// Read replicas: primary handles writes, replicas handle reads\n// Replication lag: replicas may be slightly behind (eventual consistency)\n\n// Consistent hashing: minimize data movement when adding shards\n// Hash ring: each server assigned position on ring\n// Data: hash key, find nearest clockwise server\n// Virtual nodes: each physical server has multiple ring positions\n\n// PgBouncer: PostgreSQL connection pooler\n// Transaction mode: most efficient — connection returned after each txn\n// Limits active DB connections while supporting many app connections\n\n// Database per service (microservices):\n// No cross-service DB joins — use API calls or events to combine data</code></pre>"
    },
    {
      "summary": "8.4 CAP Theorem",
      "bodyHtml": "<p><strong>Consistency:</strong> Every read returns the most recent write or an error.<br>\n<strong>Availability:</strong> Every request receives a response (not necessarily most recent).<br>\n<strong>Partition Tolerance:</strong> System continues despite network partitions.</p>\n<p>In distributed systems you must tolerate partitions. Real choice: <strong>CP vs AP</strong>.</p>\n<table>\n  <tbody><tr><th>System</th><th>Type</th><th>Trade-off</th></tr>\n  <tr><td>PostgreSQL (single node)</td><td>CA</td><td>Not distributed</td></tr>\n  <tr><td>HBase, ZooKeeper, etcd</td><td>CP</td><td>May reject reads during partition</td></tr>\n  <tr><td>Cassandra, DynamoDB, CouchDB</td><td>AP</td><td>May return stale data</td></tr>\n</tbody></table>\n<p><strong>PACELC:</strong> Even without partition, there is a Latency vs Consistency trade-off.</p>"
    },
    {
      "summary": "8.5 Architecture Patterns",
      "bodyHtml": "<pre><code>// CQRS: Command Query Responsibility Segregation\n// Commands: modify state, return nothing (or ID)\n// Queries: return data, no side effects\n// Separate read/write models optimized differently\n\n// Event Sourcing: store events, derive state by replaying them\nconst events = [\n  { type: \"Deposited\", amount: 100 },\n  { type: \"Withdrawn\", amount: 30 }\n];\nconst balance = events.reduce((acc, e) =&gt; {\n  if (e.type === \"Deposited\") return acc + e.amount;\n  if (e.type === \"Withdrawn\") return acc - e.amount;\n  return acc;\n}, 0); // 70\n\n// Outbox Pattern: guarantee message delivery\n// 1. BEGIN TRANSACTION\n// 2. INSERT business data\n// 3. INSERT INTO outbox (event_type, payload, status='pending')\n// 4. COMMIT\n// 5. Background worker: poll outbox, publish to queue, mark sent\n\n// Saga Pattern (distributed transactions):\n// Choreography: each service reacts to events from others\n// Orchestration: central coordinator issues commands to each service</code></pre>"
    },
    {
      "summary": "8.6 System Design Examples",
      "bodyHtml": "<h4>URL Shortener</h4>\n<pre><code>// Short code: Base62-encode auto-increment ID\n// 2009215674938 -&gt; \"zn9edcu\" (7 chars)\n// Schema: id (BIGINT), short_code (VARCHAR UNIQUE), original_url (TEXT), created_at\n// Cache: Redis GET shortcode -&gt; original_url (high read:write ratio)\n// Redirect: 301 (permanent, cached) vs 302 (temporary, always hits server)</code></pre>\n\n<h4>Rate Limiter</h4>\n<pre><code>// Token bucket: refill at fixed rate, burst allowed\n// Leaky bucket: requests processed at fixed rate, smooth output\n// Sliding window (Redis sorted set):\nconst now = Date.now();\nawait redis.zremrangebyscore(key, 0, now - windowMs);\nconst count = await redis.zcard(key);\nif (count &gt;= limit) throw new RateLimitError();\nawait redis.zadd(key, now, `${now}-${uuid()}`);\nawait redis.pexpire(key, windowMs);</code></pre>\n\n<h4>Chat Application</h4>\n<pre><code>// WebSocket: persistent bidirectional connection\n// Message broker (Kafka): fan-out messages to recipients\n// Presence: Redis hash { userId -&gt; socketId, lastSeen }\n// Heartbeat: client pings every 30s\n// Message delivery:\n// At-most-once: fire-and-forget (may lose messages)\n// At-least-once: retry until ack (may duplicate — use idempotency keys)\n// Exactly-once: hardest — dedup at consumer with unique message IDs</code></pre>\n\n<h4>News Feed</h4>\n<pre><code>// Fan-out on write: when Alice posts, push to all follower feeds in Redis\n//   Pro: fast reads; Con: expensive writes for celebrities (millions of followers)\n// Fan-out on read: user requests feed, server merges posts from followees\n//   Pro: cheap writes; Con: slow reads\n// Hybrid: fan-out on write for regular users, fan-out on read for celebrities</code></pre>"
    },
    {
      "summary": "8.7 Infrastructure",
      "bodyHtml": "<pre><code># Dockerfile best practices (multi-stage)\nFROM node:20-alpine AS builder\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --only=production\nCOPY . .\nRUN npm run build\n\nFROM node:20-alpine AS production\nWORKDIR /app\nRUN addgroup -S app &amp;&amp; adduser -S app -G app\nCOPY --from=builder /app/dist ./dist\nCOPY --from=builder /app/node_modules ./node_modules\nUSER app\nEXPOSE 3000\nCMD [\"node\", \"dist/server.js\"]\n\n# Kubernetes essentials:\n# Pod: smallest unit (1+ containers sharing network/storage)\n# Deployment: declarative desired state + rolling updates\n# Service: stable DNS + load balancing to pod set\n# Ingress: HTTP routing rules (path/host to service)\n# ConfigMap: non-sensitive config as key-value\n# Secret: base64 sensitive data (encrypt at rest with KMS)\n# HPA: autoscale on CPU/memory/custom metric\n\n# Deployment strategies:\n# Rolling: gradual replacement, brief mixed versions\n# Blue/Green: two envs, instant switch, double resources\n# Canary: small % to new version, monitor errors, then promote\n\n# Observability (three pillars):\n# Metrics: Prometheus scrape + Grafana dashboard\n# Logs: ELK stack or Loki + Grafana\n# Traces: OpenTelemetry -&gt; Jaeger / Tempo (distributed tracing)</code></pre>"
    }
  ],
  "keyTakeaways": {
    "title": "⚡ Key Takeaways",
    "itemsHtml": [
      "Always clarify requirements, estimate scale, and identify bottlenecks before proposing an architecture",
      "CAP theorem: distributed systems can guarantee at most two of Consistency, Availability, Partition Tolerance",
      "Horizontal scaling + stateless services + load balancer is the standard baseline for high-traffic systems",
      "CDNs serve static assets from edge nodes — they reduce latency and offload the origin significantly",
      "Database read replicas and read-through caches handle read-heavy workloads without scaling writes",
      "Idempotency keys prevent duplicate operations when clients retry failed requests",
      "Event-driven architecture decouples services but requires careful handling of ordering and at-least-once delivery"
    ]
  }
}
