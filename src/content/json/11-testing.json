{
  "slug": "11-testing",
  "headerHtml": "<h2>11. Testing (Deep) <span class=\"topic-count\">4 topics</span></h2>",
  "introHtml": "<p class=\"section-intro\">Testing knowledge signals engineering maturity. Interviews cover the testing pyramid, unit vs integration vs E2E tradeoffs, mocking strategies, and the APIs of Jest, React Testing Library, and Cypress/Playwright. Knowing what <em>not</em> to test is as valued as knowing what to test.</p>",
  "topics": [
    {
      "summary": "11.1 Test Types",
      "bodyHtml": "<table>\n  <tbody><tr><th>Type</th><th>Scope</th><th>Speed</th><th>Confidence</th><th>Maintenance</th></tr>\n  <tr><td>Unit</td><td>Single function/class</td><td>Very fast (ms)</td><td>Low (isolated)</td><td>Low</td></tr>\n  <tr><td>Integration</td><td>Multiple modules + DB/API</td><td>Seconds</td><td>Medium-High</td><td>Medium</td></tr>\n  <tr><td>E2E</td><td>Full user journey</td><td>Minutes</td><td>High</td><td>High</td></tr>\n  <tr><td>Contract</td><td>API contracts between services</td><td>Fast</td><td>Medium</td><td>Medium</td></tr>\n</tbody></table>\n<p><strong>Testing Pyramid:</strong> many unit, fewer integration, few E2E.<br>\n<strong>Testing Trophy</strong> (Kent C. Dodds): emphasize integration tests for best ROI.</p>"
    },
    {
      "summary": "11.2 Jest",
      "bodyHtml": "<pre><code>import { add } from \"./math\";\n\ndescribe(\"add()\", () =&gt; {\n  it(\"adds two numbers\", () =&gt; {\n    expect(add(1, 2)).toBe(3);\n  });\n\n  it.each([\n    [1, 2, 3],\n    [-1, 1, 0],\n    [0, 0, 0]\n  ])(\"add(%i, %i) = %i\", (a, b, expected) =&gt; {\n    expect(add(a, b)).toBe(expected);\n  });\n});\n\n// Lifecycle hooks\nbeforeAll(() =&gt; setupDB());\nafterAll(() =&gt; teardownDB());\nbeforeEach(() =&gt; clearCache());\nafterEach(() =&gt; jest.clearAllMocks());\n\n// Matchers\nexpect(42).toBe(42);\nexpect({ a: 1 }).toEqual({ a: 1 });         // deep equality\nexpect([1,2,3]).toContain(2);\nexpect(\"hello world\").toMatch(/world/);\nexpect(fn).toThrow(\"error message\");\nexpect(fn).toThrow(TypeError);\nexpect(null).toBeNull();\nexpect(arr).toHaveLength(3);\nexpect(obj).toHaveProperty(\"nested.path\", \"value\");\n\n// Mocking\nconst mockFn = jest.fn().mockReturnValue(42);\nconst asyncMock = jest.fn().mockResolvedValue({ id: 1 });\nconst rejectMock = jest.fn().mockRejectedValue(new Error(\"fail\"));\nmockFn.mockImplementation((x) =&gt; x * 2);\n\nexpect(mockFn).toHaveBeenCalledTimes(1);\nexpect(mockFn).toHaveBeenCalledWith(expect.objectContaining({ id: 1 }));\nexpect(mockFn).toHaveBeenLastCalledWith(\"arg1\");\n\n// Spy on existing method\nconst spy = jest.spyOn(console, \"error\").mockImplementation(() =&gt; {});\njest.spyOn(Date, \"now\").mockReturnValue(1234567890000);\n\n// Mock entire module\njest.mock(\"../db\", () =&gt; ({\n  query: jest.fn().mockResolvedValue([{ id: 1, name: \"Alice\" }])\n}));\n\n// Async tests\nit(\"fetches data\", async () =&gt; {\n  const data = await fetchData(1);\n  expect(data.name).toBe(\"Alice\");\n});\n\n// Coverage types:\n// Statement: each executable statement run?\n// Branch: each if/else branch taken?\n// Function: each function called?\n// Line: each line executed?\n// jest --coverage --coverageThreshold='{\"global\":{\"branches\":80}}'</code></pre>"
    },
    {
      "summary": "11.3 React Testing Library",
      "bodyHtml": "<pre><code>import { render, screen, waitFor } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\n\ntest(\"calls onClick when button clicked\", async () =&gt; {\n  const user = userEvent.setup();\n  const handleClick = jest.fn();\n\n  render(&lt;Button onClick={handleClick}&gt;Submit&lt;/Button&gt;);\n\n  await user.click(screen.getByRole(\"button\", { name: /submit/i }));\n  expect(handleClick).toHaveBeenCalledTimes(1);\n});\n\n// Query priority (most to least preferred):\n// 1. getByRole            - matches accessible role + name\n// 2. getByLabelText       - form inputs with labels\n// 3. getByPlaceholderText - input placeholders\n// 4. getByText            - text content\n// 5. getByDisplayValue    - form values\n// 6. getByAltText         - image alt text\n// 7. getByTitle           - title attribute\n// 8. getByTestId          - data-testid (last resort)\n\n// Query variants:\n// getBy    - throws if 0 or more than 1 found\n// queryBy  - returns null if not found (assert absence)\n// findBy   - async, returns promise, waits for element\n// getAllBy, queryAllBy, findAllBy - return arrays\n\n// Testing async\ntest(\"loads and displays data\", async () =&gt; {\n  render(&lt;UserProfile id={1} /&gt;);\n  expect(screen.getByText(/loading/i)).toBeInTheDocument();\n  await waitFor(() =&gt;\n    expect(screen.queryByText(/loading/i)).not.toBeInTheDocument()\n  );\n  expect(screen.getByText(\"Alice\")).toBeInTheDocument();\n});\n\n// User interactions\nawait user.type(input, \"hello world\");\nawait user.clear(input);\nawait user.selectOptions(select, [\"option1\"]);\nawait user.keyboard(\"{Enter}\");\nawait user.tab();</code></pre>"
    },
    {
      "summary": "11.4 Cypress / Playwright",
      "bodyHtml": "<pre><code>// Playwright E2E test\nimport { test, expect } from \"@playwright/test\";\n\ntest(\"user can log in\", async ({ page }) =&gt; {\n  await page.goto(\"/login\");\n  await page.getByLabel(\"Email\").fill(\"user@test.com\");\n  await page.getByLabel(\"Password\").fill(\"password123\");\n  await page.getByRole(\"button\", { name: /log in/i }).click();\n  await expect(page).toHaveURL(\"/dashboard\");\n  await expect(page.getByText(\"Welcome back\")).toBeVisible();\n});\n\ntest(\"shows error on API failure\", async ({ page }) =&gt; {\n  await page.route(\"**/api/users\", route =&gt;\n    route.fulfill({ status: 500, body: \"Server Error\" })\n  );\n  await page.goto(\"/users\");\n  await expect(page.getByText(/failed to load/i)).toBeVisible();\n});\n\n// Selector best practices (most stable to least):\n// 1. data-testid: data-testid=\"submit-btn\"\n// 2. ARIA role: page.getByRole(\"button\", { name: /submit/i })\n// 3. Labels: page.getByLabel(\"Email\")\n// 4. Text: page.getByText(\"Submit\")\n// Avoid: CSS classes (.btn-primary — unstable, style-coupled)\n\n// Cypress equivalent\ncy.visit(\"/login\");\ncy.findByLabelText(\"Email\").type(\"user@test.com\");\ncy.findByRole(\"button\", { name: /login/i }).click();\ncy.url().should(\"include\", \"/dashboard\");\ncy.intercept(\"GET\", \"/api/users\", { statusCode: 500 }).as(\"usersError\");</code></pre>"
    }
  ],
  "keyTakeaways": {
    "title": "⚡ Key Takeaways",
    "itemsHtml": [
      "Test the behavior users see, not implementation details; prefer <code>getByRole</code> over <code>getByTestId</code>",
      "Mock at system boundaries (HTTP calls, file system, timers), not internal functions",
      "<code>beforeEach</code> setup and <code>afterEach</code> cleanup prevent test pollution; tests should be independent and order-agnostic",
      "Snapshot tests catch regressions but become noise when updated blindly — use them sparingly",
      "E2E tests are the slowest and most brittle layer — focus them on critical user flows, not exhaustive coverage",
      "Coverage percentage is a proxy metric; 100% coverage does not mean 100% correct behavior"
    ]
  }
}
